Start using javascript templates.
Move most of the javascript logic into it's own library
Clean up the API
Need to be able to get events from the API
Filtering (search and details)
More config options / less hard-coding.
Clean up naming on index
Allow multiple detail streams (stdout/stderr)
